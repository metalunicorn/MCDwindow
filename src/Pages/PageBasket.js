import React, { useState, useEffect, useRef, useCallback } from "react";
import { CBasket } from "../Components/Basket";

export const PageBasket = () => {
    return (
    <>
        <h1>Basket</h1>
        <CBasket/>
    </>
    );
}